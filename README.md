# Hashing

## How to install

1. Add the package ```philipschlender/hashing-php``` to the ```require``` or ```require-dev``` element of the ```composer.json``` file

    ```bash
    composer require philipschlender/hashing-php
    ```
